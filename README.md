# tiom.nu
Website for my company. 

Hosted on Cloudflare Pages.

- `_headers` contains security and cache headers used in production.
- `_redirects` contains canonical domain and path redirects.
- `.htaccess` is for Apache only and is not used by Cloudflare Pages.
